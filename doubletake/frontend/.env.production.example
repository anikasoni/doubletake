# Copy to .env.production and set to your deployed backend's base URL.
# When set to a full URL, the frontend calls the backend directly (the Vite
# dev proxy is only used when VITE_API_BASE is unset).
VITE_API_BASE=https://your-backend-url-here
